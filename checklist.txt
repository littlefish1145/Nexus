# Checklist

> 每个检查项对应 tasks.md 中任务完成后的验收标准。所有项必须勾选才算改进落地。

## 阶段 1：生态兼容

- [ ] SSE-C：`aws s3 cp` 走 `x-amz-server-side-encryption-customer-*` 头能成功上传与下载
- [ ] SSE-C：MD5 不匹配的请求被网关拒绝（400）
- [ ] SSE-C：密钥在审计日志中以 `***REDACTED***` 出现
- [ ] SSE-C：禁用 SSE-C 路径下的去重
- [ ] 事件通知：`PutObject` 后 webhook 收到带 `X-Nexus-Signature` 的 POST
- [ ] 事件通知：5xx 响应触发指数退避重试
- [ ] 事件通知：死信队列落盘 + 监控指标可查询
- [ ] 事件通知：同 key 顺序保证（chaos test 验证）
- [ ] CLI：`nexusctl bucket list --output json` 输出合法 JSON
- [ ] CLI：`--query` JMESPath 表达式生效
- [ ] CLI：bash / zsh / fish / powershell 补全脚本可加载
- [ ] CLI：配置文件用 OS keyring 加密凭据

## 阶段 2：生产可用

- [ ] Prometheus：`/metrics` 端点暴露核心指标（≥ 50 个 metric）
- [ ] Prometheus：histogram bucket 数量 ≤ 7
- [ ] OpenTelemetry：OTLP gRPC exporter 工作正常（本地 Tempo/Jaeger 验证）
- [ ] OpenTelemetry：跨加密微服务 trace 链路完整（gateway → coordinator → keygen → encrypt）
- [ ] 健康检查：`/healthz` `/readyz` `/livez` 行为符合 k8s probe 语义
- [ ] 日志：slog 输出 JSON，含 `trace_id` / `span_id`
- [ ] KMS：Vault Transit 后端能生成/解包 DEK
- [ ] KMS：AWS KMS 后端能生成/解包 DEK
- [ ] KMS：`nexusctl crypto rotate` 触发后旧 DEK 仍可读、新 DEK 用新 key
- [ ] KMS：KMS 不可用时降级策略符合文档
- [ ] 配置：`nexusctl config validate` 能在启动前捕获类型错误
- [ ] 配置：跨字段约束（dim vs model）启动期校验通过
- [ ] 配置：SIGHUP 热加载仅对白名单字段生效

## 阶段 3：集群能力

- [ ] Raft：3 节点集群能选主并接受写
- [ ] Raft：kill leader 后 follower 5s 内选出新 leader
- [ ] Raft：网络分区时不出现双 leader（pre-vote + check quorum 验证）
- [ ] Raft：单节点 → 3 节点平滑升级工具不丢数据
- [ ] 存储后端：`S3Backend` 能在端到端测试中 PUT/GET 一个对象
- [ ] 存储后端：`AzureBlobBackend` 同上
- [ ] 存储后端：纠删码（k=4, m=2）在 2 节点故障时数据可恢复
- [ ] 存储后端：桶级 `storage_class` 路由到正确 backend
- [ ] 备份：增量备份包大小 < 全量的 10%（典型工作负载）
- [ ] 备份：备份包异地加密 + S3 上传
- [ ] 备份：`nexusctl backup restore` 能在新实例上恢复并通过 conformance 测试
- [ ] 备份：`nexusctl backup drill` 演练成功率指标可观测

## 阶段 4：差异化能力

- [ ] 全文：`/admin/fts/search?q=hello` 返回 BM25 排序结果
- [ ] 全文：snippet / highlight 字段正确
- [ ] 全文：compaction 任务定期运行且不阻塞查询
- [ ] 全文：hybrid search（BM25 + 向量）融合分数 RRF 可调
- [ ] 向量：HNSW mmap 索引文件创建并可查询
- [ ] 向量：SQ 量化召回率 ≥ 99%，PQ ≥ 95%（标准数据集）
- [ ] 向量：进程重启后索引加载时间 < 重建时间 10%
- [ ] 向量：索引损坏时检测并触发重建
- [ ] 断点续传：客户端断线后能通过 HEAD 拿到 offset 继续 PATCH
- [ ] 断点续传：session 过期后 `.tmp` 被清理
- [ ] 断点续传：完成时正确触发管线、加密、向量索引
- [ ] 断点续传：与 S3 multipart 协议共存（feature flag）
- [ ] IAM：条件键 `StringEquals` 策略评估通过测试
- [ ] IAM：ABAC 跨 tag 策略能 Allow / Deny 符合预期
- [ ] IAM：Permission Boundary 限制生效
- [ ] IAM：SCP 在 org 级护栏生效
- [ ] IAM：`nexusctl iam simulate` 输出 Allow/Deny + 命中语句
- [ ] IAM：condition evaluator fuzz 1h 无崩溃

## 阶段 5：生态建设

- [ ] Go SDK：`import "github.com/nexus/sdk-go/s3"` 能用，boto3 风格 API 兼容
- [ ] Go SDK：boto3 风格 `boto3.client('s3', endpoint_url=...)` 兼容
- [ ] SDK：s3-test-suite 通过率 ≥ 95%
- [ ] SDK：版本兼容策略文档发布
- [ ] 文档：MkDocs 站点可构建，主题正常
- [ ] 文档：proto → MarkDown 文档自动生成
- [ ] 文档：OpenAPI 3.0 spec 文件存在并校验通过
- [ ] 文档：部署、运维、API 参考、性能、安全白皮书齐全
- [ ] 部署：systemd unit 启动后所有微服务健康
- [ ] 部署：docker-compose `up` 后主进程 + 7 微服务全部就绪
- [ ] 部署：Helm chart `helm install` 在 minikube 上成功
- [ ] 部署：multi-stage 镜像 < 100MB

## 横切质量

- [ ] 测试：`go test ./... -race` 通过
- [ ] 测试：新增代码覆盖率 ≥ 80%
- [ ] 测试：密码学代码 100% 覆盖
- [ ] 测试：CI（GitHub Actions）每次 PR 必跑
- [ ] 性能：PUT P99 < 100ms（10KB 对象，本地）
- [ ] 性能：GET P99 < 50ms（10KB 对象，本地）
- [ ] 性能：加密 RPC P99 < 10ms
- [ ] 性能：向量检索 P99 < 50ms（10w 向量，HNSW）
- [ ] 安全：NIST CAVP test vector 通过
- [ ] 安全：fuzz 跑 ≥ 1h 无崩溃
- [ ] 可观测：核心指标数 ≥ 50
- [ ] 可观测：trace 覆盖率（关键路径）≥ 90%
